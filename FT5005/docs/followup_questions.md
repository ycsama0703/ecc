# Remaining Follow-Up Questions For Kelvin And Yu Ta

These are the most useful open questions after their latest reply.

1. Do you also have 5-minute benchmark data such as `SPY` or `DIA` for the same sample period? If not, we will proceed with raw intraday returns first, but benchmark data would still be useful for later market-adjusted return and event-study extensions.

2. Is there any file that lists which events are missing in `A3` or `A4`, or ideally the exact usable intersection across `A2`, `A3`, `A4`, `C1`, and `D`? This would help us estimate the effective sample size much faster.

3. For the larger `SP500` or `SP1500` version, are we allowed to compute embeddings onsite and take home only embeddings, labels, and non-restricted metadata, even if the raw text and audio cannot be shared?
